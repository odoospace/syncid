<odoo>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="syncid.list">
      <field name="name">syncid list</field>
      <field name="model">syncid.syncid</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="syncid.action_window">
      <field name="name">syncid window</field>
      <field name="res_model">syncid.syncid</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="syncid.action_server">
      <field name="name">syncid server</field>
      <field name="model_id" ref="model_syncid_syncid"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="syncid" id="syncid.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="syncid.menu_1" parent="syncid.menu_root"/>
    <menuitem name="Menu 2" id="syncid.menu_2" parent="syncid.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="syncid.menu_1_list" parent="syncid.menu_1"
              action="syncid.action_window"/>
    <menuitem name="Server to list" id="syncid" parent="syncid.menu_2"
              action="syncid.action_server"/>
    -->
</odoo>
